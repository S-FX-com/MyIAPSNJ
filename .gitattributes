# Files excluded from `git archive`, which is how the release workflow builds
# the installable plugin zip. Anything listed here never ships to a site.
/.github        export-ignore
/.gitattributes export-ignore
/.gitignore     export-ignore
/CLAUDE.md      export-ignore
